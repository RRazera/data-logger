<!DOCTYPE html>

<html>
    <head>
        <title>Home</title>
        <link rel="stylesheet" href="./css/styles.css">
        <script src="js/app.js"></script>
    </head>
    
    <body>
        <div class="main-content">
            {{>header}}
        </div>

        <div class="experiments">
            <ul>
                {{#each experiments}}
                    <li> <a href="/experiments/{{this._id}}">Policarboxilato</a></li>
                {{/each}}
            </ul>
        </div>

        {{>footer}}
    </body>
</html>
